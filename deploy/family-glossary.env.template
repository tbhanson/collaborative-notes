# /etc/family-glossary.env
# Copy this file to the server at /etc/family-glossary.env
# chmod 600 /etc/family-glossary.env  (readable only by root)
#
# Generate a secret with:  openssl rand -hex 32

SESSION_SECRET=replace-with-output-of-openssl-rand-hex-32
DATABASE_PATH=/var/lib/family-glossary/glossary.db
PORT=8080
